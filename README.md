# agent-abide-dev

Development branch of the asdaaas infrastructure. Once stable, replaces live/comms as production.

## Structure
- core/ — asdaaas.py, backends, config
- api/ — HTTP API server (messages endpoint, WebSocket live tail)
- tests/ — test suite

